# Copy to .env and point at the deployed backend for production builds
# (e.g. Netlify). Defaults to http://127.0.0.1:8000 for local development.
VITE_API_BASE_URL=https://gprabhanjana-oral-cancer-cbir-api.hf.space
